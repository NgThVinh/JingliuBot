// const { GUILD_ID } = require('../../utils/constants.js')

module.exports = {
	name: 'userUpdate',
	async execute(oldUser, newUser, interaction, client) {
        const oldStream = oldUser.presence.activities.find(activity => activity.type === 'STREAMING');
        const newStream = newUser.presence.activities.find(activity => activity.type === 'STREAMING');

        if (!oldStream && newStream) {
            // bắt đầu tính thời gian
        } else if (oldStream && !newStream) {
            // cộng thời gian vào db
        }
    },
};